import { AbstractDBModel } from "./AbstractDBModel"
export const _categoryModel = {
    name:'',
    parent:''
  }

  export const categoryModel = Object.assign(_categoryModel, AbstractDBModel);