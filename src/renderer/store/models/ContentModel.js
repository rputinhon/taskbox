
import { AbstractDBModel } from "./AbstractDBModel"

export const _contentModel = {
    type: '',
    hint:null,
    data: null
}

export const contentModel = Object.assign(_contentModel, AbstractDBModel);