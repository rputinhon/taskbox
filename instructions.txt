Taskbox uses VUE, ELECTRON, NODEJS as bases.

unzip the source.

open command window:
cd 'src folder'
npm install
npm run electron:serve / for development
npm run electron:build / for deployment

programas para editar os codigos usado VISUAL CODE.

server

install couch db
create an  username:admin / password:admin server administrator
create a db name 'taskbox'

in the taskbox db permissions area, down in the member area, put:
Role: 'worker'

the same for the _user db.
Role: 'worker'

enable CORS

configuration couchdb sample

[admins]
register  admin:admin

[couchdb]
uuid = ...
users_db_security_editable = true
single_node = true

[couch_httpd_auth]
secret = ...
allow_persistent_cookies = 

[cors]
origins = *
methods = GET, PUT, POST, HEAD, DELETE
credentials = true
headers = accept,authorization,content-type, origin, referer

[httpd]
enable_cors = true
WWW-Authenticate = 
require_valid_user = true

[couch_peruser]
enabled = true

[chttpd]
bind_address = 0.0.0.0
port = 5984
authentication_handlers = 

[chttpd_auth]
timeout = 28800
allow_persistent_cookies = true
same-site = lax

[couch_httpd]
timeout = 

[smoosh.overnight_channel]
strict_window = true
from = 22:00
to = 6:00

        